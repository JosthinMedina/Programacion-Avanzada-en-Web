@model IEnumerable<SistemaGestionAcademica.Web.Models.Asistencia>

@using SistemaGestionAcademica.Web.Models

@{
    ViewData["Title"] = "Asistencias";

    var matriculas =
        ViewBag.Matriculas as List<Matricula>
        ?? new List<Matricula>();

    var estudiantes =
        ViewBag.Estudiantes as List<Estudiante>
        ?? new List<Estudiante>();

    var cursos =
        ViewBag.Cursos as List<Curso>
        ?? new List<Curso>();
}

<div class="container-fluid mt-4">

    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show">

            @TempData["Success"]

            <button type="button"
                    class="btn-close"
                    data-bs-dismiss="alert">
            </button>

        </div>
    }

    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show">

            @TempData["Error"]

            <button type="button"
                    class="btn-close"
                    data-bs-dismiss="alert">
            </button>

        </div>
    }

    <div class="d-flex justify-content-between align-items-center mb-4">

        <div>

            <h2 class="fw-bold">
                Gestión de Asistencias
            </h2>

            <small class="text-muted">
                Registro de asistencia de los estudiantes.
            </small>

        </div>

        <a asp-action="Registrar"
           class="btn btn-primary">

            <i class="bi bi-plus-circle me-1"></i>

            Nueva Asistencia

        </a>

    </div>

    <div class="card shadow border-0">

        <div class="card-body">

            @if (!Model.Any())
            {
                <div class="alert alert-info mb-0">
                    No hay asistencias registradas.
                </div>
            }
            else
            {
                <div class="table-responsive">

                    <table class="table table-hover align-middle">

                        <thead class="table-dark">

                            <tr>
                                <th>ID</th>
                                <th>Matrícula</th>
                                <th>Estudiante</th>
                                <th>Curso</th>
                                <th>Fecha</th>
                                <th>Estado</th>
                                <th>Observación</th>
                                <th width="190">Acciones</th>
                            </tr>

                        </thead>

                        <tbody>

                            @foreach (var item in Model)
                            {
                                var matricula =
                                    matriculas.FirstOrDefault(
                                        x => x.IdMatricula ==
                                             item.IdMatricula
                                    );

                                var estudiante = matricula == null
                                    ? null
                                    : estudiantes.FirstOrDefault(
                                        x => x.IdEstudiante ==
                                             matricula.IdEstudiante
                                    );

                                var curso = matricula == null
                                    ? null
                                    : cursos.FirstOrDefault(
                                        x => x.IdCurso ==
                                             matricula.IdCurso
                                    );

                                <tr>

                                    <td>
                                        @item.IdAsistencia
                                    </td>

                                    <td>
                                        #@item.IdMatricula
                                    </td>

                                    <td>

                                        @if (estudiante != null)
                                        {
                                            <span>
                                                @estudiante.Nombre
                                                @estudiante.PrimerApellido
                                                @estudiante.SegundoApellido
                                            </span>
                                        }
                                        else
                                        {
                                            <span class="text-muted">
                                                Sin información
                                            </span>
                                        }

                                    </td>

                                    <td>

                                        @if (curso != null)
                                        {
                                            @curso.NombreCurso
                                        }
                                        else
                                        {
                                            <span class="text-muted">
                                                Sin información
                                            </span>
                                        }

                                    </td>

                                    <td>
                                        @item.Fecha.ToString("dd/MM/yyyy")
                                    </td>

                                    <td>

                                        @if (item.Estado == "Presente")
                                        {
                                            <span class="badge bg-success">
                                                Presente
                                            </span>
                                        }
                                        else if (item.Estado == "Ausente")
                                        {
                                            <span class="badge bg-danger">
                                                Ausente
                                            </span>
                                        }
                                        else
                                        {
                                            <span class="badge bg-warning text-dark">
                                                Justificada
                                            </span>
                                        }

                                    </td>

                                    <td>

                                        @if (string.IsNullOrWhiteSpace(
                                            item.Observacion))
                                        {
                                            <span class="text-muted">
                                                Sin observación
                                            </span>
                                        }
                                        else
                                        {
                                            @item.Observacion
                                        }

                                    </td>

                                    <td>

                                        <a asp-action="Editar"
                                           asp-route-id="@item.IdAsistencia"
                                           class="btn btn-warning btn-sm">

                                            <i class="bi bi-pencil-square"></i>

                                            Editar

                                        </a>

                                        <a asp-action="Eliminar"
                                           asp-route-id="@item.IdAsistencia"
                                           class="btn btn-danger btn-sm"
                                           onclick="return confirm('¿Desea eliminar esta asistencia?')">

                                            <i class="bi bi-trash"></i>

                                            Eliminar

                                        </a>

                                    </td>

                                </tr>
                            }

                        </tbody>

                    </table>

                </div>
            }

        </div>

    </div>

</div>
